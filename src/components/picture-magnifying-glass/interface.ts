export interface MoveInfoProps {
    left: number,
    top: number,
    pos: DOMRect | null
}

export interface ConfigProps {
    maskColor: string,
    maskOpacity: number,
    scale: number,
    width: number,
    height: number
}